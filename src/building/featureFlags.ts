// 功能旗標：用來關掉上游功能而不刪它的程式碼。
//
// 刪掉上游的東西會讓日後合併上游更新變得很痛；改成用旗標關掉，
// 想恢復只要把值改回 true，也不會產生合併衝突。

/**
 * 上游的「狀態 1~5 ／ 加權」。
 *
 * 關掉的理由：裝備切換已經由我們的「裝備組 Set 1~5」負責，而狀態存的其實是
 * 實戰資料與 BUFF 快照（見 src/stores/stateSlots.ts），跟裝備是兩條正交的軸，
 * 同時擺在工具列上只會讓人以為是同一件事。
 *
 * 邏輯全部保留：stateSlots store、加權頁、以及它們的計算都沒有動，
 * 這裡只是不把入口顯示出來。
 *
 * 注意：App.vue 在加權啟用時會短路成只渲染加權頁。入口藏起來之後，
 * 若有人先前正停在加權狀態就會出不去，所以那個短路也要一併用這個旗標擋掉。
 */
export const SHOW_STATE_SLOTS = false
